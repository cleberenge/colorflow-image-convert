
import NotFound from '../src/pages/NotFound'

export const metadata = {
  title: 'Página não encontrada - ChoicePDF',
  description: 'A página que você procura não foi encontrada'
}

export default function NotFoundPage() {
  return <NotFound />
}
